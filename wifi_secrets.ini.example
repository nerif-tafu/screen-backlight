; Copy to wifi_secrets.ini (gitignored) to bake Wi-Fi credentials into firmware.
; Prefer configuring Wi-Fi via the web UI after first boot.

[env:seeed_xiao_esp32c3]
build_flags =
    -DWIFI_SSID=\"your-ssid\"
    -DWIFI_PASSWORD=\"your-password\"
